import { createElement } from "../../hooks/createElement";

const RenderBonusPoint = createElement(
    'span',
    { 
      id: `loyalty-points`,
      className: 'text-blue-500 ml-2',
    }
);

export { RenderBonusPoint };